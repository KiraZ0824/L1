# 追加奖励审核页面功能说明

| 功能点 | 说明 |
|--------|------|
| 审核编号 | 规则 `RW-` + 两位自增序号 + 项目编号，示例 `RW-01PD01202604001`，发起时自动生成不可编辑，标题不展示编号只展示【追加奖励】+项目名称 |
| 项目编号 | 规则 `P` + 部门代码 + 6位年月 + 三位序号，示例 `PD01202604001`，部门代码使用组织架构编码（技术部 D01、产品部 D02、合规部 C、市场部 H 等） |
| 审核面板布局 | 顺序：审核编号 → 项目 → 申请人 → 事项描述 → 奖励分配列表 → 审核流程 → 审核意见 → 操作按钮 |
| 审核流程 | 只展示当前状态节点（等待审核 或 审核通过），不展示发起申请和驳回节点 |
| 奖励分配列表 | 展示人员/奖励值/原因，三列 flex:1 等分，不自适应，奖励值左对齐；金额可编辑，修改后合计行实时更新 |
| 审核操作 | 审核意见必填，审核通过扣除项目预算 |
| 待办列表 | Todo 页面展示待审核申请可点击审核；Backlog 编号列展示审核编号（如 `RW-01PD01202604001`，不是 `BR-br1`），标题列展示【追加奖励】+项目名称 |
| 消息通知 | 未读消息：待审核状态通知；已读消息：已通过/已驳回两种状态通知 |
| 数据模型 | 项目新增 `projectCode` 字段；申请数据含 `reviewCode`(审核编号)、`allocations`(分配列表)、`status`(pending/approved/rejected)、`opinion`(审核意见) |