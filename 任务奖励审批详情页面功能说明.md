# 任务奖励审批详情页面功能说明

## 一、功能概述

任务完成并提交审核后，若 `reward > 0`，进入奖励审批流程，由审批人分配奖励并审核（通过/不通过）。

## 二、触发条件

| 功能点 | 说明 |
|--------|------|
| 任务状态 | `pending_review` |
| 奖励值 | `reward > 0` |
| 入口函数 | `auditTaskReward(taskId)` |

## 三、入口与跳转

| 功能点 | 说明 |
|--------|------|
| 待办列表（Backlog） | 显示 `【任务奖励】` + 标题，编号列显示审批编号 `RW-` + taskId，优先级列显示 `-`，点击打开审批面板 |
| Overview 待审核 | 同上，显示 `【任务奖励】` + 标题 + 编号，点击打开审批面板 |
| 任务详情页 | 已验收 + 有奖励 → 显示"查看奖励审批详情"链接，根据状态分流到 `auditTaskReward` / `auditTask` / `openTaskDetail` |

## 四、审批面板结构

### 4.1 面板标题栏

| 功能点 | 说明 |
|--------|------|
| 图标 | SVG 勾选图标，橙色 |
| 标题 | `【任务奖励】` + 任务标题 |
| 审批编号 | 等宽灰色字体，跟在标题后，如 `RW-TASKTC1220260530001` |

### 4.2 任务详情区

| 功能点 | 说明 |
|--------|------|
| 任务编号 | 等宽灰色，如 `TASKTC1220260530001`，与状态同行 |
| 状态 | 颜色圆点 + 中文状态（绿=已验收，蓝=进行中，红=驳回中，灰=其他） |
| 延期标记 | 仅 `in_progress` 超期时显示红色"⚠️ 延期" |
| 任务标题 | 加粗，字段标签"任务标题" |
| 优先级 | 颜色圆点 + P0/P1/P2/P3 |
| 所属项目 | 项目名称，无则 `-` |
| 所属阶段 | 阶段名称，无则 `-` |
| 项目负责人 | 项目负责人姓名，取自项目 `lead` 字段 |
| 截止日期 | 超期显示红色"已延期" |
| 任务负责人 | 必填 |
| 奖励 (RP) | 橙色加粗，无则 `-` |
| 交付形式 | 无则显示"无需交付" |
| 任务描述 | 有条件，仅 `t.desc` 存在时展示 |
| 审核流程 | 垂直时间线：提交审核→等待审核/审核通过/审核驳回→等待重新提交 |
| 完成时间 | 有条件，仅 `t.completedAt` 存在时展示 |

### 4.3 审核流程节点

| 功能点 | 说明 |
|--------|------|
| 提交审核 | 始终显示，负责人首字母 + 橙色边框，已完成 |
| 等待审核 | `pending_review` 时显示，灰色圆圈，待处理 |
| 审核通过 | `done` 时显示，绿色 ✓，已通过 |
| 审核驳回 | `rejected` 时显示，红色首字母，已驳回 |
| 等待重新提交 | `rejected` 时显示，灰色圆圈，待处理 |

### 4.4 奖励分配区

| 功能点 | 说明 |
|--------|------|
| 标题 | 奖励分配 + 说明"默认按预计奖励平均分配，改动需填原因" |
| 分配行-人员 | 下拉选择框，预填项目成员 |
| 分配行-金额 | 数字输入框，默认平均值，余数归第一个成员 |
| 分配行-原因 | 文本输入框，金额变动时必填 |

### 4.5 审核意见区

| 功能点 | 说明 |
|--------|------|
| 标签 | 审核意见（红色星号必填） |
| 输入框 | 多行文本框，placeholder"填写审核通过或驳回的意见..." |

### 4.6 操作按钮

| 功能点 | 说明 |
|--------|------|
| 取消 | 幽灵按钮，关闭面板 |
| 审核不通过 | 红色边框按钮，调用 `doRejectAudit()`，状态改为 `rejected` |
| 审核通过 | 橙色主按钮，调用 `doApproveAudit()`，生成审批编号 |

## 五、审核流程

### 5.1 审核通过

| 功能点 | 说明 |
|--------|------|
| 校验审核意见 | 必填，否则提示 |
| 校验奖励分配 | 金额变动未填原因则提示 |
| 计算总额 | 累加所有分配行 |
| 保存分配明细 | `{ person, amount, reason }` 数组 |
| 更新任务 | `reward = totalAllocated`，`rewardAllocation = allocations`，`status = 'done'`，`auditNo = 'RW-' + taskId` |
| 刷新视图 | `refreshAllTaskViews()` |
| 关闭面板 | `closeProjectDetail()` |
| 提示结果 | alert 显示审批编号和奖励总额 |

### 5.2 审核不通过

| 功能点 | 说明 |
|--------|------|
| 校验审核意见 | 必填，否则提示 |
| 更新状态 | `status = 'rejected'` |
| 刷新视图 | `refreshAllTaskViews()` |
| 关闭面板 | `closeProjectDetail()` |

## 六、数据模型

### 6.1 Task 完整字段

| 功能点 | 说明 |
|--------|------|
| `id` | 任务唯一标识，如 `'t23'` |
| `taskId` | 任务编号，如 `'TASKTC1220260530001'` |
| `title` | 任务标题 |
| `status` | 状态：draft/todo/in_progress/pending_review/rejected/done/closed |
| `priority` | 优先级：P0/P1/P2/P3 |
| `reward` | 奖励值 RP，0 表示无奖励 |
| `rewardAllocation` | 奖励分配明细 `[{ person, amount, reason }]` |
| `auditNo` | 审批编号，如 `'RW-TASKTC1220260530001'` |
| `assignee` | 任务负责人 |
| `auditor` | 审核人 |
| `projectId` | 所属项目ID |
| `backlog` | 所属阶段名称 |
| `deadline` | 截止日期 |
| `delivery` | 交付形式 |
| `desc` | 任务描述（可选） |
| `createdAt` | 创建时间 |
| `updatedAt` | 更新时间 |
| `completedAt` | 完成时间（可选） |
| `submitTime` | 提交审核时间 |
| `approveTime` | 审核通过时间 |
| `rejectTime` | 审核驳回时间 |
| `rejectReason` | 驳回原因 |
| `approveComment` | 审核通过意见 |

### 6.2 任务编号生成规则

| 功能点 | 说明 |
|--------|------|
| 格式 | `TASK` + 部门码(2位) + 组号(2位) + 日期(8位) + 流水号(3位) |
| 示例 | `TASKTC1220260530001` |
| 部门代码 | TC=技术部, PD=产品部, CP=合规部, FN=财务部, B0=BD, MK=市场部, OP=运维部, HR=人事部, GM=总经办, SI=战略投资部 |
| 生成函数 | `generateTaskNumber(dept)` |

### 6.3 审批编号生成规则

| 功能点 | 说明 |
|--------|------|
| 格式 | `RW-` + 任务编号 |
| 示例 | `RW-TASKTC1220260530001` |
| 生成时机 | 审核通过时写入 `t.auditNo` |
| 对照 | 知识奖励审批编号为 `RW-` + 知识编号（如 `RW-K00TC01001001`） |

## 七、相关函数

| 功能点 | 说明 |
|--------|------|
| `auditTaskReward(id)` | 打开任务奖励审批面板 |
| `_taskDetailHtml(t)` | 渲染任务详情区字段 |
| `_taskAuditFlowHtml(t)` | 渲染审核流程时间线 |
| `_buildRewardRow(person, share)` | 构建单行奖励分配 |
| `doApproveAudit()` | 审核通过处理 |
| `doRejectAudit()` | 审核不通过处理 |
| `openRewardAuditDetail(refId)` | 奖励审批详情路由 |
| `generateTaskNumber(dept)` | 生成任务编号 |
| `_showTaskPanel(title, body)` | 展示侧边面板 |
| `refreshAllTaskViews()` | 刷新任务视图 |
| `closeProjectDetail()` | 关闭面板 |

## 八、与知识奖励审批对照

| 功能点 | 任务奖励审批 | 知识奖励审批 |
|--------|-------------|-------------|
| 数据对象 | 协作任务（Task） | 知识审核记录（KnowledgeReview） |
| 触发函数 | `auditTaskReward(id)` | `auditRewardTask(id)` |
| 面板标题 | `【任务奖励】`+ 标题 + 审批编号 | `【知识奖励】`+ 二级标签 + `奖励审核` + 审批编号 |
| 审批编号 | `RW-` + taskId | `RW-` + keyId |
| 奖励分配人 | 项目成员 | 知识贡献者 |
| 触发条件 | `pending_review` + `reward > 0` | `done` + `reward > 0` |
| 审核通过 | 状态改 `done`，生成审批编号 | 状态保持 `done`，生成知识编号和审批编号 |
| 待办编号列 | 审批编号 `RW-` + taskId | 审批编号 `RW-` + keyId |
| 待办优先级列 | `-` | `-` |